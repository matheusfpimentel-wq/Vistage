// O padrão auto-grow agora mora em `@/components/ui/auto-grow-textarea` (fonte
// única, regra transversal §0). Mantido aqui só como reexport pra não quebrar os
// importadores existentes das Festas.
export { AutoGrowTextarea as AutoGrowInput } from "@/components/ui/auto-grow-textarea";
